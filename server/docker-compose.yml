version: "3.8"
services:
  trip-itinerary-checklist:
    image: node:20-alpine
    container_name: trip-itinerary-checklist
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "3000:3000"
    command: sh -c "npm install && npm start"
    restart: unless-stopped
